import MainView from "@/components/mealPlanSection/MainRecipeView";

// `app/page.tsx` is the UI for the `/` URL
export default function Page() {
  return (
    <div className="px-20">
      <MainView />
    </div>
  );
}
